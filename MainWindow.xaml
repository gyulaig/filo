<Window x:Class="Filo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Filo"
        mc:Ignorable="d"
        Title="Filo - File Manager" Height="600" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <!-- Toolbar -->
        <ToolBar Grid.Row="1" Padding="5">
            <Button Name="ToolbarBackButton" Click="ToolbarBackButton_Click" Content="◀" ToolTip="Back" Width="30"/>
            <Button Name="ToolbarForwardButton" Click="ToolbarForwardButton_Click" Content="▶" ToolTip="Forward" Width="30"/>
            <Button Name="ToolbarDirUpButton" Click="ToolbarDirUpButton_Click" Content="↑" ToolTip="Up" Width="30"/>
            <Separator/>
            <Button Name="ToolbarNewDirButton" Click="ToolbarNewDirButton_Click" Content="New Folder" Padding="5,2"/>
            <Button Name="ToolbarDeleteButton" Click="ToolbarDeleteButton_Click" Content="Delete" Padding="5,2"/>
            <Separator/>
            <TextBox Name="ToolbarPathTextBox" KeyDown="ToolbarPathTextBox_KeyDown" Width="400" VerticalContentAlignment="Center" Text="C:\Users\Documents"/>
            <Button Name="ToolbarPathGoButton" Click="ToolbarPathGoButton_Click" Content="Go" Padding="5,2"/>
        </ToolBar>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TreeView Name="FolderTreeView" Grid.Column="0" Margin="5" SelectedItemChanged="TreeView_SelectedItemChanged">
                <!-- populated from code -->
            </TreeView>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="LightGray"/>

            <ListView Name="FileList" Grid.Column="2" Margin="5" MouseDoubleClick="ListView_MouseDoubleClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Icon}" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Date Modified" Width="150" DisplayMemberBinding="{Binding DateModified}"/>
                        <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding Type}"/>
                        <GridViewColumn Header="Size" Width="100" DisplayMemberBinding="{Binding Size}"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </Grid>

        <StatusBar Name="MainStatusBar" Grid.Row="3">
            <StatusBarItem>
                <TextBlock Name="MainStatusBarItemsText" Text="4 items"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Name="MainStatusBarSizeText" Text="7.8 MB"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>